server {
    listen 80;
    server_name localhost;

    location /static/ {
        alias /usr/share/nginx/html/static/;
        autoindex off;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

}
